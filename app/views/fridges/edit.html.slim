p.navigation = link_to 'Back', kitchen_board_path

h1 = t('heading.edit', model: @fridge.class.model_name.human)

== render 'form'

p.navigation = link_to 'Delete this fridge', fridge_path(@fridge), method: :delete
